# Raw datasets drop location for Byte pipeline
Place any raw .jsonl or .json fine-tuning datasets into this folder.
The automated pipeline script (`python3 training/pipeline.py`) will recursively scan, validate, format, deduplicate, and version all datasets placed here.
